PRAGMA foreign_keys = ON;

CREATE TABLE IF NOT EXISTS "with_number_primary_field" (
  "field_primary_number" INTEGER NOT NULL PRIMARY KEY
);

CREATE TABLE IF NOT EXISTS "with_string_primary_field" (
  "field_primary_string" TEXT NOT NULL PRIMARY KEY
);


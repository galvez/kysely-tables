
CREATE TABLE IF NOT EXISTS "with_string_fields" (
  "field_string" varchar(255) NOT NULL,
  "field_string_large" varchar(255) NOT NULL,
  "field_string_sized" varchar(255) NOT NULL,
  "field_string_nullable" varchar(255),
  "field_string_large_nullable" varchar(255),
  "field_string_sized_nullable" varchar(255)
);

